---
description: 'Merge the current Chimera branch to main via the git merge skill. Usage: /merge-to-main'
agent: 'Chimera Git Operations'
---

Load and follow `.github/skills/git/merge/SKILL.md`.

Run the merge skill script:

```bash
bash .github/skills/git/merge/scripts/check-and-merge.sh
```

Report the result briefly.
