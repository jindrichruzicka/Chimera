// Thin re-export of the engine root layout from @chimera-engine/renderer. The app owns
// the route file; the shell (html/body, AppShell, the provider + bootstrap tree,
// GameRegistrationBootstrap) ships from the package dist.
export { default, metadata } from '@chimera-engine/renderer/shell/layout';
