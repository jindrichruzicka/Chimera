node_modules/
dist/
build/
out/
.next/
pnpm-lock.yaml
*.min.js
*.min.css
renderer/out/
coverage/
.e2e-build/

# Legacy Electron bundle outputs — source lives in the adjacent .ts files
electron/main/index.js
electron/preload/api.js

# Playwright output — generated artefacts, not source
e2e/playwright-report/
test-results/

# Machine-generated toolchain snapshot (tools/gen-toolchain.ts). Prettier-ignored so the
# verify:toolchain-snapshot drift gate can byte-compare it against the generator's own output.
tools/create-chimera-game/toolchain.generated.ts

# Changesets prerelease-mode state, rewritten by `changeset version`/`pre` in its own JSON
# style. Prettier-ignored so a prerelease (rc) iteration never trips the format gate.
.changeset/pre.json
