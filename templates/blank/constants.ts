// __Game Title__'s identity + action ids — the single place that names the game
// id. Every other module imports it from here.

/** This game's stable id (also its content/data root and settings namespace). */
export const __GAME_CONSTANT___GAME_ID = '__game_kebab__';

/** Example action id — replace with your game's real actions. */
export const __GAME_CONSTANT___PING_ACTION = '__game_kebab__:ping';
